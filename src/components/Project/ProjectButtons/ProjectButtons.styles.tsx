import { IconButton, styled } from '@mui/material';

export const ProjectSwitchButton = styled(IconButton)({
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  width: '5rem',
  height: '100%',
  borderRadius: 8
});
