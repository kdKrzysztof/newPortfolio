import { styled } from '@mui/material';
import { Box } from '@mui/material';

export const StyledBox = styled(Box)({
  position: 'absolute',
  width: '100%',
  height: '100%',
  opacity: '30%',
  zIndex: '-1'
});
