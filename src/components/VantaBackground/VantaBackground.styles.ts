import { styled } from '@mui/material';
import { Box } from '@mui/material';

export const StyledBox = styled(Box)({
  position: 'fixed',
  width: '100%',
  height: '100%',
  opacity: '25%',
  zIndex: '-1'
});
